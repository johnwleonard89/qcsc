---
// Embedded Google Form with your branding
---

<div class="quote-form-container">
	<div class="form-header">
		<h3>Request a Free Quote</h3>
		<p>Get a personalized estimate for your surface coating project</p>
	</div>
	
	<div class="form-button-wrapper">
		<a href="https://docs.google.com/forms/d/e/1FAIpQLSc1WyoJJmD5-CXbuLNvJdbyeyEJ1CCmbGQQmq1kQueTCsAUyzw/viewform" 
		   target="_blank" 
		   class="quote-form-button">
			Get My Free Quote →
		</a>
		<p class="button-note">Opens in a new tab • Takes just 2 minutes to complete</p>
	</div>
	
	<p class="form-note">
		By submitting this form, you agree to be contacted by Queen City Surface Coatings regarding your quote request.
	</p>
</div>

<style>
/* Quote Request Form Container - Navy & Yellow Theme */
.quote-form-container {
	background: #002F4B;
	padding: 2rem;
	border-radius: var(--theme-shape-radius);
	box-shadow: 0 4px 16px var(--theme-shadow-light);
	max-width: 100%;
}

.form-button-wrapper {
	text-align: center;
	margin: 2rem 0;
}

.quote-form-button {
	display: inline-block;
	background: #FFC107;
	color: #002F4B;
	padding: 1rem 2rem;
	border-radius: var(--theme-shape-radius);
	text-decoration: none;
	font-size: 1.125rem;
	font-weight: 600;
	font-family: var(--theme-font-family-sans);
	transition: var(--theme-transition);
	box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
	border: none;
	cursor: pointer;
	min-height: 48px;
	line-height: 1.4;
}

.quote-form-button:hover {
	background: #E6AC00;
	color: #002F4B;
	transform: translateY(-2px);
	box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
	text-decoration: none;
}

.button-note {
	color: white;
	opacity: 0.8;
	font-size: 0.875rem;
	margin-top: 1rem;
	line-height: 1.4;
}

.form-header {
	text-align: center;
	margin-bottom: 1rem;
}

.form-header h3 {
	color: #FFC107;
	margin-bottom: 1rem;
	font-size: clamp(1.5rem, 4vw, 2rem);
	font-weight: 700;
	font-family: var(--theme-font-family-serif);
}

.form-header p {
	color: white;
	opacity: 0.9;
	line-height: 1.6;
	margin: 0;
}

.form-row {
	display: grid;
	grid-template-columns: 1fr 1fr;
	gap: 1rem;
}

.form-note {
	font-size: 0.875rem;
	color: white;
	opacity: 0.8;
	text-align: center;
	line-height: 1.4;
	margin-top: 0.5rem;
}

.quote-submit-button {
	width: 100%;
	margin: 1rem auto 0 auto;
	display: block;
	text-align: center;
}

/* Form components styling */
:global(.quote-form .form-input),
:global(.quote-form .form-select),
:global(.quote-form .form-textarea) {
	min-height: 44px;
	font-size: 16px;
	border-radius: var(--theme-shape-radius);
	padding: 0.75rem 1rem;
	border: 2px solid rgba(255, 255, 255, 0.3);
	background: rgba(255, 255, 255, 0.1);
	color: white;
	transition: var(--theme-transition);
	font-family: var(--theme-font-family-sans);
}

:global(.quote-form .form-input::placeholder),
:global(.quote-form .form-textarea::placeholder) {
	color: rgba(255, 255, 255, 0.6);
}

:global(.quote-form .form-input:focus),
:global(.quote-form .form-select:focus),
:global(.quote-form .form-textarea:focus) {
	outline: none;
	border-color: #FFC107;
	box-shadow: 0 0 0 3px rgba(255, 193, 7, 0.3);
}

:global(.quote-form .form-textarea) {
	min-height: 120px;
	resize: vertical;
}

:global(.quote-form button) {
	width: 100%;
	min-height: 48px;
	background: #FFC107 !important;
	color: #002F4B !important;
	border: none;
	border-radius: var(--theme-button-border-radius);
	font-size: 1rem;
	font-weight: 600;
	font-family: var(--theme-font-family-sans);
	cursor: pointer;
	transition: var(--theme-transition);
	box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
	margin: 1rem auto 0 auto;
	display: block;
	text-align: center;
}

:global(.quote-form button:hover) {
	background: #E6AC00 !important;
	color: #002F4B !important;
	transform: translateY(-2px);
	box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
}

:global(.quote-form label) {
	font-weight: 600;
	color: white;
	margin-bottom: 0.5rem;
	display: block;
	font-size: 0.875rem;
}

/* Success Message Styling */
.form-success {
	background: #002F4B;
	padding: 2rem;
	border-radius: var(--theme-shape-radius);
	box-shadow: 0 4px 16px var(--theme-shadow-light);
	text-align: center;
	color: white;
}

.form-success h3 {
	color: #FFC107;
	font-size: 1.5rem;
	margin-bottom: 1rem;
	font-weight: 700;
}

.form-success p {
	margin-bottom: 1rem;
	line-height: 1.6;
}

.success-contact a {
	color: #FFC107;
	text-decoration: none;
	font-weight: 600;
}

.success-contact a:hover {
	text-decoration: underline;
}


/* Mobile Responsive */
@media (max-width: 768px) {
	.form-row {
		grid-template-columns: 1fr;
	}
	
	.quote-form {
		padding: 1.5rem;
	}
	
	.form-header h3 {
		font-size: 1.5rem;
	}
	
	.form-header {
		margin-bottom: 1.5rem;
	}
}
</style>


