---
import CountyServiceTemplate from '../../../templates/CountyServiceTemplate.astro';
import { counties, services } from '../../../data/locations';

const county = counties['kenton-county-kentucky'];
const service = services['metallic-epoxy-flooring'];

const relatedCounties = [
	counties['hamilton-county-ohio'],
	counties['butler-county-ohio'],
	counties['warren-county-ohio'],
	counties['campbell-county-kentucky']
];

const relatedServices = [
	services['garage-floor-epoxy'],
	services['basement-floor-coating'],
	services['polyaspartic-coating'],
	services['commercial-concrete-coating']
];
---

<CountyServiceTemplate {county} {service} {relatedCounties} {relatedServices} />