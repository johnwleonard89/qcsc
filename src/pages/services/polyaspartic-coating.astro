---
import PremiumServiceTemplate from '../../templates/PremiumServiceTemplate.astro';
import { residentialServices } from '../../data/premiumServiceData.js';

const pageData = residentialServices.polyasparticCoating;
---

<PremiumServiceTemplate 
  seo={pageData.seo}
  hero={pageData.hero}
  intro={pageData.intro}
  differentiators={pageData.differentiators}
  process={pageData.process}
  collections={pageData.collections}
  portfolio={pageData.portfolio}
  contact={pageData.contact}
/>